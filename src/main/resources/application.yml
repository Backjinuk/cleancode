spring:
  application:
    name: cleancode

  datasource:
    url: 'jdbc:h2:file:/db/H2/cleancode;AUTO_SERVER=true'
    username: 'sa'
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false  # SQL? ??
    properties:
      hibernate:
        format_sql: false  # SQL? ?? ?? ??

  # SQL ?? ??? ??? ?? (?? ??)
  logging:
    level:
      org.hibernate.SQL: DEBUG  # SQL ?? ?? ?? ??
      org.hibernate.type.descriptor.sql.BasicBinder: TRACE  # ???? ????? ??? ??

  sql:
    init:
      mode: always  # SQL